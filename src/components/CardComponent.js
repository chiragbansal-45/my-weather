import React from 'react';
import { Media } from 'reactstrap';

const WeatherInfo = (props) => {
    return(
        <div>
            <div>
                <div>
                    <Media object src="assets/images/Clear.svg" alt="image not found" ></Media>
                </div>
            </div>
        </div>
    );
}

export default WeatherInfo;